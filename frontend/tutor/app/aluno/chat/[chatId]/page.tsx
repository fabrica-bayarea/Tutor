'use client';

import { useState } from 'react';

export default function Chat() {
    const [chat, setChat] = useState({});
    return (
        <div>
            <h1>Chat</h1>
        </div>
    );
}
